name: test_color_project
description: Test Color project for Solid Software.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^1.2.5

  flutter_bloc: ^6.1.1  

  dartz: ^0.9.2

  get_it: ^5.0.3

  http: ^0.12.2
  
  data_connection_checker: ^0.3.4

  connectivity: ^2.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.3  


flutter:

  uses-material-design: true

  
